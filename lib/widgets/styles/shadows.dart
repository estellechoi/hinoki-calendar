import 'package:flutter/material.dart';
import '../styles/colors.dart' as colors;

final input = [
  BoxShadow(
      color: colors.shadow,
      blurRadius: 25,
      spreadRadius: 0,
      offset: Offset(0, 5)),
  BoxShadow(
      color: colors.shadowDeep,
      blurRadius: 20,
      spreadRadius: 0,
      offset: Offset(0, 0))
];

final tool = [
  BoxShadow(
      color: colors.shadowDark,
      blurRadius: 4,
      spreadRadius: 0,
      offset: Offset(-2, 0))
];

final card = [
  BoxShadow(
      color: colors.shadow,
      blurRadius: 25,
      spreadRadius: 0,
      offset: Offset(0, 5)),
  BoxShadow(
      color: colors.shadowDeep,
      blurRadius: 20,
      spreadRadius: 0,
      offset: Offset(0, 0))
];

final calendarStrongcell = [
  BoxShadow(
      color: colors.shadowDarker,
      blurRadius: 1,
      spreadRadius: 0,
      offset: Offset(0, 1))
];

final bottomModal = [
  BoxShadow(
      color: colors.shadowDeep,
      blurRadius: 5,
      spreadRadius: 0,
      offset: Offset(0, -4)),
  BoxShadow(
      color: colors.white, blurRadius: 5, spreadRadius: 0, offset: Offset(0, 4))
];

final guideCategorySectionTitle = [
  BoxShadow(
      color: colors.shadowDarker,
      blurRadius: 4,
      spreadRadius: 0,
      offset: Offset(0, 2)),
];

// drop-shadow(0 1px 1px rgba(0, 0, 0, 0.25))
